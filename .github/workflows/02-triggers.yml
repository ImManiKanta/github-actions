name: triggers
on:
  # push:
  #   branches:
  #     - 'main'
  workflow_dispatch #manually we need to trigger the workflow
# [push] # Triggers the workflow on every push to the repository

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
    - name: main branch  
      run: echo "manually trigger branch"
    # steps:
    #   - name: main branch  
    #     run: echo "main branch push"